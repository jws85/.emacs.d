;; I would have avoided using obvious "marketing lingo" -- but I'm
;; natively a programmer, who every now and again is compelled to go
;; and mangle system configurations in order to get something higher
;; up the stack working again.  I'm not really a sysadmin... and would
;; call this sort of thing 'devops', such as it is.

;; Hipster VMs!  (Ahem, hipster chroots.)
(use-package dockerfile-mode
  :ensure t
  :mode "Dockerfile\\'")

(provide 'jws-devops)
