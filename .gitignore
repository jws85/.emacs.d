# site-specific .emacs.d customizations
custom.el
site-init.el
site-lisp/

# Where I put the source for Emacs itself; I'll often run long-term
# support releases, but still want the latest stuff, which means self-
# compiling
src/*

# master emacs auto-generated directory -- all of the stuff
# below should be getting shunted into this directory instead
#
# I did say *should* -- as of now (2013-11-22) there is a bug of
# some ilk in projectile that prevents caching of its data between
# emacs restarts unless the cache file is living in ~/.emacs.d
# toplevel.  Ugh.  I like projectile too much to change.
.cache/

# I shouldn't need to block these anymore but I have them here
# just in case.  Probably the biggest weakness of emacs IMHO is its
# propensity to crap files everywhere; I am anal enough about it that
# it really sticks in my craw.

# auto-complete
ac-comphist.dat

# auto-save-list
auto-save-list/

# bookmarks file
bookmarks

# emms installation
emms/

# elpa installation
elpa/

# emoji installation
emojis/

# eshell installation
eshell/

# games (e.g. Tetris scores)
games/

# infodocs
info/

# eww installation
eww-bookmarks
url/

# helm adaptive sort
helm-adaptive-history

# ido cruft
ido.last
smex-items

# Something to do with emacs' IME
kkcrc

# multiple-cursors
.mc-lists.el

# places file
places

# projectile
projectile.cache
projectile-bookmarks.eld

# recentf
recentf

# tramp cache
tramp

# junk files
*~
session.*
tags
TAGS